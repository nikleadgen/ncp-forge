# Launch Observations Log

Human-readable companion to `.claude/observations/{date}.ndjson`. Each
entry is one line appended by `/log-observation`. Reverse-chronological
(newest at the bottom).

Format: `YYYY-MM-DD HH:MM | session-label | text`

---

<!-- entries appended below this line -->
